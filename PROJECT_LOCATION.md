# NAJM Active Project

Canonical local path:

`/Users/monti/Projects/NAJM`

Canonical remote:

`https://github.com/iDh88/najm-deploy-test.git`

Active branch:

`main`

Post-consolidation release tag:

`v1.0.0-post-consolidation`

Rules:

1. This is the only active local NAJM repository.
2. Do not work from Downloads, Desktop archives, extracted folders, or legacy copies.
3. Before starting work:
   - `git switch main`
   - `git pull origin main`
   - `git status`
4. Create a new branch for every feature or fix.
5. Do not commit directly to main.
6. Do not commit .env files, API keys, credentials, certificates, generated caches, or build outputs.
7. The legacy repository is preserved temporarily at:
   `/Users/monti/Projects/NAJM_ARCHIVE/legacy-najm-complete`
8. The legacy archive is not an active development repository.
